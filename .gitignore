_archivo/
*.credentials.json
.credentials.json
.env
.env.local
**/.insforge/
project.json
reports/

# Local skill loads (scripts/load-skills.sh --apply). Runtime copies are never
# canonical and must not be committed.
.claude/skills/
